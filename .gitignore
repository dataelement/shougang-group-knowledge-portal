.omx/

# macOS
.DS_Store

# Editor metadata
# Ignore everything under .claude/ except the team-facing handover doc that
# CLAUDE.md §0 links to. Using .claude/* (not .claude/) so the ! re-include works.
.claude/*
!.claude/HANDOVER.md
.idea/
.vscode/

# Root-level local captures
screenshots/

# Asset source libraries (curated raw assets, do not pack into build)
assets/sggf-source/

# Wiki ETL 源 xlsx 不入库；产物 frontend/src/data/wikiData.json 入 git
assets/*.xlsx

# Backend
backend/.env
backend/.pytest_cache/
backend/.venv/
backend/app/config/data/bisheng_runtime.json
backend/**/__pycache__/
backend/**/*.py[cod]

# Frontend
frontend/node_modules/
frontend/dist/
frontend/.test-dist*/
frontend/*.local
