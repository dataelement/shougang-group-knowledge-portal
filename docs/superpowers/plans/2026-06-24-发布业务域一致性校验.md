# 发布业务域一致性校验 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在门户侧提供「文件 → 目标公共知识空间」的业务域一致性校验能力，并把后台业务域「绑定空间」候选收敛为公共知识空间。

**Architecture:** 校验逻辑内聚在门户后端一个无副作用的服务（解析文件编码第 3 段得到文件业务域 code，反查绑定目标空间的业务域 code 集合，命中任一即一致），通过一个 precheck 接口暴露给发布编排方调用（"门户校验通过后再调毕昇"）。前端只改后台业务域编辑弹窗的绑定空间候选过滤与保存校验。

**Tech Stack:** 后端 FastAPI + Pydantic + pytest；前端 React + TypeScript，测试用 `node --test`（编译自 `tsconfig.tests.json`）。

## Global Constraints

- 文件业务域 code = 文件编码（`KnowledgeFileItem.file_encoding`，格式 `SGGF-STD-{CODE}-{SEQ}`）按 `-` 切分后的**第 3 段**（索引 2），`strip().upper()`。
- 比较前所有 code 统一 `strip().upper()`。
- 文件 code 无法解析、目标空间未配置业务域 → **一律禁止发布**（不放行、不静默跳过）。
- 一个空间被多个业务域绑定时，文件 code **命中任一**即视为一致。
- `reason_code` 取值固定为：`OK` / `FILE_CODE_MISSING` / `SPACE_DOMAIN_UNCONFIGURED` / `DOMAIN_MISMATCH`。
- 本计划**不**实现「调用毕昇发布」与门户发布前端入口（PRD §11 开放项，待入口/毕昇 API 确认后另起）。precheck 接口即「门户校验」交付物。
- 后端测试运行：`cd backend && python -m pytest <文件>::<用例> -v`（如环境用 uv，则 `cd backend && uv run pytest …`）。
- 前端测试运行：`cd frontend && npm test`（编译并执行全部 `frontend/tests/*.test.ts`）。

---

## File Structure

- `backend/app/services/domain_consistency_service.py`（新建）— `parse_domain_code()` 纯函数 + `DomainConsistencyService.check()`。
- `backend/app/schemas/knowledge.py`（修改）— 新增 `DomainRef` / `PublishPrecheckRequest` / `PublishPrecheckResult`。
- `backend/app/api/routes/knowledge.py`（修改）— 新增 `POST /api/v1/knowledge/publish/precheck`。
- `backend/tests/test_domain_consistency_service.py`（新建）— 服务单测。
- `backend/tests/test_publish_precheck_api.py`（新建）— 接口单测。
- `frontend/src/utils/adminDomains.ts`（修改）— 新增 `getPublicSpaceOptions()`，`validateDomainDraft()` 增加公共空间校验。
- `frontend/src/pages/AdminPage.tsx`（修改）— 业务域编辑弹窗绑定空间下拉用 `getPublicSpaceOptions()`。
- `frontend/tests/adminDomains.test.ts`（修改）— 新增/更新用例。

---

### Task 1: 文件编码解析（parse_domain_code）

**Files:**
- Create: `backend/app/services/domain_consistency_service.py`
- Test: `backend/tests/test_domain_consistency_service.py`

**Interfaces:**
- Produces: `parse_domain_code(file_encoding: str) -> str`（返回第 3 段大写，缺失/格式非法返回 `""`）。

- [ ] **Step 1: 写失败测试**

创建 `backend/tests/test_domain_consistency_service.py`：

```python
from app.services.domain_consistency_service import parse_domain_code


def test_parse_domain_code_extracts_third_segment():
    assert parse_domain_code("SGGF-STD-PP-001") == "PP"


def test_parse_domain_code_uppercases_and_strips():
    assert parse_domain_code("  sggf-std-qm-1 ") == "QM"


def test_parse_domain_code_returns_empty_when_missing():
    assert parse_domain_code("") == ""


def test_parse_domain_code_returns_empty_when_malformed():
    assert parse_domain_code("SGGF-STD-PP") == ""   # 缺少序列段
    assert parse_domain_code("SGGF-STD--001") == ""  # 第3段为空
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd backend && python -m pytest tests/test_domain_consistency_service.py -v`
Expected: FAIL（`ModuleNotFoundError: app.services.domain_consistency_service`）

- [ ] **Step 3: 写最小实现**

创建 `backend/app/services/domain_consistency_service.py`：

```python
from __future__ import annotations


def parse_domain_code(file_encoding: str) -> str:
    """从文件编码提取业务域 code（第 3 段）。

    文件编码格式 ``SGGF-STD-{CODE}-{SEQ}``，如 ``SGGF-STD-PP-001`` -> ``PP``。
    缺失或格式非法时返回空串。
    """
    if not file_encoding:
        return ""
    parts = [segment.strip() for segment in file_encoding.strip().split("-")]
    if len(parts) < 4 or not parts[2]:
        return ""
    return parts[2].upper()
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd backend && python -m pytest tests/test_domain_consistency_service.py -v`
Expected: PASS（4 passed）

- [ ] **Step 5: 提交**

```bash
git add backend/app/services/domain_consistency_service.py backend/tests/test_domain_consistency_service.py
git commit -m "feat(domain-check): parse business-domain code from file encoding"
```

---

### Task 2: 一致性校验服务（DomainConsistencyService.check）

**Files:**
- Modify: `backend/app/schemas/knowledge.py`（新增 3 个 schema）
- Modify: `backend/app/services/domain_consistency_service.py`（新增 service 类）
- Test: `backend/tests/test_domain_consistency_service.py`（追加用例）

**Interfaces:**
- Consumes: `parse_domain_code`（Task 1）；`DomainConfig`（`app.schemas.portal_config`，字段 `name/space_ids/code/enabled`）。
- Produces:
  - `DomainRef(code: str, name: str)`
  - `PublishPrecheckResult(allowed: bool, reason_code: str, message: str, file_domain: Optional[DomainRef], space_domains: list[DomainRef])`
  - `DomainConsistencyService().check(file_encoding: str, target_space_id: int, domains: Iterable[DomainConfig]) -> PublishPrecheckResult`

- [ ] **Step 1: 新增 schema**

在 `backend/app/schemas/knowledge.py` 顶部把 `from typing import Literal` 改为：

```python
from typing import Literal, Optional
```

在文件末尾追加：

```python
class DomainRef(BaseModel):
    code: str
    name: str


class PublishPrecheckRequest(BaseModel):
    file_encoding: str = ""
    target_space_id: int


class PublishPrecheckResult(BaseModel):
    allowed: bool
    reason_code: str
    message: str
    file_domain: Optional[DomainRef] = None
    space_domains: list[DomainRef] = Field(default_factory=list)
```

- [ ] **Step 2: 写失败测试**

在 `backend/tests/test_domain_consistency_service.py` 追加：

```python
from app.services.domain_consistency_service import DomainConsistencyService
from app.schemas.portal_config import DomainConfig


def _domain(name, code, space_ids, enabled=True):
    return DomainConfig(
        name=name, code=code, space_ids=space_ids,
        color="#1", bg="#2", icon="Factory", background_image="", enabled=enabled,
    )


def test_check_allows_when_file_code_matches_space_domain():
    domains = [_domain("生产", "PP", [104])]
    result = DomainConsistencyService().check("SGGF-STD-PP-001", 104, domains)
    assert result.allowed is True
    assert result.reason_code == "OK"
    assert result.file_domain.code == "PP"


def test_check_blocks_on_domain_mismatch():
    domains = [_domain("能源", "EM", [110]), _domain("生产", "PP", [104])]
    result = DomainConsistencyService().check("SGGF-STD-PP-001", 110, domains)
    assert result.allowed is False
    assert result.reason_code == "DOMAIN_MISMATCH"
    assert "生产" in result.message and "能源" in result.message


def test_check_blocks_when_file_code_missing():
    domains = [_domain("生产", "PP", [104])]
    result = DomainConsistencyService().check("", 104, domains)
    assert result.allowed is False
    assert result.reason_code == "FILE_CODE_MISSING"


def test_check_blocks_when_space_has_no_domain():
    domains = [_domain("生产", "PP", [104])]
    result = DomainConsistencyService().check("SGGF-STD-PP-001", 999, domains)
    assert result.allowed is False
    assert result.reason_code == "SPACE_DOMAIN_UNCONFIGURED"


def test_check_allows_when_space_bound_by_multiple_domains_hits_any():
    domains = [_domain("财务", "FI", [104]), _domain("生产", "PP", [104])]
    result = DomainConsistencyService().check("SGGF-STD-PP-001", 104, domains)
    assert result.allowed is True


def test_check_ignores_disabled_domains():
    domains = [_domain("生产", "PP", [104], enabled=False)]
    result = DomainConsistencyService().check("SGGF-STD-PP-001", 104, domains)
    assert result.reason_code == "SPACE_DOMAIN_UNCONFIGURED"
```

- [ ] **Step 3: 运行测试确认失败**

Run: `cd backend && python -m pytest tests/test_domain_consistency_service.py -v`
Expected: FAIL（`ImportError: cannot import name 'DomainConsistencyService'`）

- [ ] **Step 4: 写实现**

在 `backend/app/services/domain_consistency_service.py` 顶部补充 import，并追加 service 类：

```python
from typing import Iterable

from app.schemas.knowledge import DomainRef, PublishPrecheckResult
from app.schemas.portal_config import DomainConfig
```

```python
class DomainConsistencyService:
    def check(
        self,
        file_encoding: str,
        target_space_id: int,
        domains: Iterable[DomainConfig],
    ) -> PublishPrecheckResult:
        domains = list(domains)
        file_code = parse_domain_code(file_encoding)
        if not file_code:
            return PublishPrecheckResult(
                allowed=False,
                reason_code="FILE_CODE_MISSING",
                message="文件编码缺失或不规范，无法识别业务域，请先补全文件编码后再发布。",
                file_domain=None,
                space_domains=[],
            )

        space_domains = [
            d for d in domains
            if d.enabled and d.code.strip() and target_space_id in d.space_ids
        ]
        space_refs = [DomainRef(code=d.code.strip().upper(), name=d.name) for d in space_domains]
        space_codes = {ref.code for ref in space_refs}

        file_domain_name = next(
            (d.name for d in domains if d.code.strip().upper() == file_code),
            file_code,
        )
        file_ref = DomainRef(code=file_code, name=file_domain_name)

        if not space_codes:
            return PublishPrecheckResult(
                allowed=False,
                reason_code="SPACE_DOMAIN_UNCONFIGURED",
                message="目标公共空间未配置业务域，请联系管理员在后台为该空间绑定业务域后再发布。",
                file_domain=file_ref,
                space_domains=[],
            )

        if file_code in space_codes:
            return PublishPrecheckResult(
                allowed=True,
                reason_code="OK",
                message="",
                file_domain=file_ref,
                space_domains=space_refs,
            )

        space_names = "、".join(ref.name for ref in space_refs)
        return PublishPrecheckResult(
            allowed=False,
            reason_code="DOMAIN_MISMATCH",
            message=f"该文件属于「{file_domain_name}」业务域，与目标空间「{space_names}」业务域不一致，不允许发布。",
            file_domain=file_ref,
            space_domains=space_refs,
        )
```

- [ ] **Step 5: 运行测试确认通过**

Run: `cd backend && python -m pytest tests/test_domain_consistency_service.py -v`
Expected: PASS（全部用例 passed）

- [ ] **Step 6: 提交**

```bash
git add backend/app/services/domain_consistency_service.py backend/app/schemas/knowledge.py backend/tests/test_domain_consistency_service.py
git commit -m "feat(domain-check): add DomainConsistencyService.check with precheck schemas"
```

---

### Task 3: 发布预校验接口（POST /publish/precheck）

**Files:**
- Modify: `backend/app/api/routes/knowledge.py`
- Test: `backend/tests/test_publish_precheck_api.py`

**Interfaces:**
- Consumes: `DomainConsistencyService`（Task 2）；`PublishPrecheckRequest`（Task 2）；`get_portal_config_service`、`response_ok`（已有）。
- Produces: HTTP `POST /api/v1/knowledge/publish/precheck`，body `{ file_encoding, target_space_id }`，返回 `response_ok(PublishPrecheckResult)`。

- [ ] **Step 1: 写失败测试**

创建 `backend/tests/test_publish_precheck_api.py`：

```python
from fastapi.testclient import TestClient

from app.api.dependencies import get_portal_config_service
from app.main import app
from app.schemas.portal_config import DomainsConfigUpdate
from app.services.portal_config_service import PortalConfigService


def _seed_config(tmp_path):
    config_service = PortalConfigService(config_path=tmp_path / "portal.json")
    config_service.update_domains(
        DomainsConfigUpdate(
            domains=[
                {"name": "生产", "space_ids": [104], "color": "#1", "bg": "#2",
                 "icon": "Factory", "background_image": "", "enabled": True, "code": "PP"},
                {"name": "能源", "space_ids": [110], "color": "#1", "bg": "#2",
                 "icon": "Zap", "background_image": "", "enabled": True, "code": "EM"},
            ]
        )
    )
    return config_service


def test_precheck_allows_matching_domain(tmp_path):
    config_service = _seed_config(tmp_path)
    app.dependency_overrides[get_portal_config_service] = lambda: config_service
    try:
        client = TestClient(app)
        resp = client.post(
            "/api/v1/knowledge/publish/precheck",
            json={"file_encoding": "SGGF-STD-PP-001", "target_space_id": 104},
        )
        assert resp.status_code == 200
        data = resp.json()["data"]
        assert data["allowed"] is True
        assert data["reason_code"] == "OK"
    finally:
        app.dependency_overrides.clear()


def test_precheck_blocks_mismatch(tmp_path):
    config_service = _seed_config(tmp_path)
    app.dependency_overrides[get_portal_config_service] = lambda: config_service
    try:
        client = TestClient(app)
        resp = client.post(
            "/api/v1/knowledge/publish/precheck",
            json={"file_encoding": "SGGF-STD-PP-001", "target_space_id": 110},
        )
        assert resp.status_code == 200
        data = resp.json()["data"]
        assert data["allowed"] is False
        assert data["reason_code"] == "DOMAIN_MISMATCH"
    finally:
        app.dependency_overrides.clear()
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd backend && python -m pytest tests/test_publish_precheck_api.py -v`
Expected: FAIL（404，路由不存在）

- [ ] **Step 3: 写实现**

在 `backend/app/api/routes/knowledge.py` 的 import 区块补充：

```python
from app.services.domain_consistency_service import DomainConsistencyService
```

并把 `from app.schemas.knowledge import (...)` 列表加入 `PublishPrecheckRequest`：

```python
from app.schemas.knowledge import (
    DocumentFileChatRequest,
    FavoriteDocumentRequest,
    FilePreviewSourceKind,
    HomeStatsData,
    PublishPrecheckRequest,
    ShareDocumentAccessRequest,
    ShareDocumentRequest,
)
```

在 `router` 定义之后追加路由：

```python
@router.post("/publish/precheck")
async def publish_precheck(
    payload: PublishPrecheckRequest,
    portal_config_service: PortalConfigService = Depends(get_portal_config_service),
):
    config = portal_config_service.get_config()
    result = DomainConsistencyService().check(
        payload.file_encoding,
        payload.target_space_id,
        config.domains,
    )
    return response_ok(result.model_dump())
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd backend && python -m pytest tests/test_publish_precheck_api.py -v`
Expected: PASS（2 passed）

- [ ] **Step 5: 全量后端回归**

Run: `cd backend && python -m pytest -q`
Expected: 全绿（无回归）

- [ ] **Step 6: 提交**

```bash
git add backend/app/api/routes/knowledge.py backend/tests/test_publish_precheck_api.py
git commit -m "feat(domain-check): add POST /knowledge/publish/precheck endpoint"
```

---

### Task 4: 业务域绑定空间下拉收敛为公共空间

**Files:**
- Modify: `frontend/src/utils/adminDomains.ts`（新增 `getPublicSpaceOptions`）
- Modify: `frontend/src/pages/AdminPage.tsx`（下拉用 `getPublicSpaceOptions`）
- Test: `frontend/tests/adminDomains.test.ts`（新增用例）

**Interfaces:**
- Consumes: `SpaceConfig`（`../api/adminConfig`，含 `space_level?: string`）。
- Produces: `getPublicSpaceOptions(spaces: SpaceConfig[]): SpaceConfig[]`（仅保留 `space_level === 'public'`）。

- [ ] **Step 1: 写失败测试**

在 `frontend/tests/adminDomains.test.ts` 顶部 import 追加 `getPublicSpaceOptions`：

```ts
import { createDomainDraft, isSelectedDomainColor, validateDomainDraft, getPublicSpaceOptions, DOMAIN_CODE_OPTIONS } from '../src/utils/adminDomains';
```

追加用例：

```ts
test('getPublicSpaceOptions keeps only public spaces', () => {
  const result = getPublicSpaceOptions([
    { id: 1, name: '营销', file_count: 0, tag_count: 0, enabled: true, space_level: 'public' },
    { id: 2, name: '我的库', file_count: 0, tag_count: 0, enabled: true, space_level: 'personal' },
    { id: 3, name: '部门库', file_count: 0, tag_count: 0, enabled: true, space_level: 'department' },
  ]);
  assert.deepEqual(result.map((s) => s.id), [1]);
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd frontend && npm test`
Expected: FAIL（编译报错：`getPublicSpaceOptions` 未导出）

- [ ] **Step 3: 写实现**

在 `frontend/src/utils/adminDomains.ts` 末尾追加：

```ts
export function getPublicSpaceOptions(spaces: SpaceConfig[]): SpaceConfig[] {
  return spaces.filter((space) => (space.space_level ?? '').trim().toLowerCase() === 'public');
}
```

在 `frontend/src/pages/AdminPage.tsx` 顶部确认已从 `../utils/adminDomains` 导入；将 `DomainEditorDialog` 绑定空间下拉（约 1869-1875 行）：

```tsx
              <option value="">未绑定（暂不上首页）</option>
              {spaces.map((space) => (
```

改为：

```tsx
              <option value="">未绑定（暂不上首页）</option>
              {getPublicSpaceOptions(spaces).map((space) => (
```

（同文件 import 处加入 `getPublicSpaceOptions`。）

- [ ] **Step 4: 运行测试确认通过**

Run: `cd frontend && npm test`
Expected: PASS（含 `getPublicSpaceOptions keeps only public spaces`）

- [ ] **Step 5: 提交**

```bash
git add frontend/src/utils/adminDomains.ts frontend/src/pages/AdminPage.tsx frontend/tests/adminDomains.test.ts
git commit -m "feat(admin): restrict domain binding picker to public spaces"
```

---

### Task 5: 保存时校验绑定空间必须为公共空间

**Files:**
- Modify: `frontend/src/utils/adminDomains.ts`（`validateDomainDraft` 增加公共空间校验）
- Test: `frontend/tests/adminDomains.test.ts`（新增拒绝用例 + 更新既有成功用例 fixture）

**Interfaces:**
- Consumes: `getPublicSpaceOptions` 同源的 `space_level` 判定。
- Produces: `validateDomainDraft` 在绑定非公共空间时返回 `{ error: '绑定空间必须是公共知识空间' }`。

- [ ] **Step 1: 写失败测试 + 更新既有 fixture**

在 `frontend/tests/adminDomains.test.ts` 中，把 `validateDomainDraft returns a domain config incl. uppercased code` 用例里的 spaces fixture 补上 `space_level: 'public'`：

```ts
  }, [
    { id: 18, name: '冷轧技术手册', file_count: 10, tag_count: 0, enabled: true, space_level: 'public' },
  ]);
```

追加拒绝用例：

```ts
test('validateDomainDraft rejects binding to a non-public space', () => {
  const result = validateDomainDraft({
    name: '能源',
    spaceId: '20',
    icon: 'Zap',
    backgroundImage: '',
    color: '#d97706',
    bg: '#fef3c7',
    enabled: true,
    code: 'EM',
  }, [
    { id: 20, name: '部门库', file_count: 0, tag_count: 0, enabled: true, space_level: 'department' },
  ]);
  assert.equal(result.error, '绑定空间必须是公共知识空间');
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd frontend && npm test`
Expected: FAIL（新拒绝用例未返回该 error）

- [ ] **Step 3: 写实现**

在 `frontend/src/utils/adminDomains.ts` 的 `validateDomainDraft` 中，把现有空间存在性校验块：

```ts
    if (!spaces.some((space) => space.id === spaceId)) return { error: '绑定空间不存在' };
    spaceIds = [spaceId];
```

改为：

```ts
    const boundSpace = spaces.find((space) => space.id === spaceId);
    if (!boundSpace) return { error: '绑定空间不存在' };
    if ((boundSpace.space_level ?? '').trim().toLowerCase() !== 'public') {
      return { error: '绑定空间必须是公共知识空间' };
    }
    spaceIds = [spaceId];
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd frontend && npm test`
Expected: PASS（全部前端用例通过，含新拒绝用例与更新后的成功用例）

- [ ] **Step 5: 提交**

```bash
git add frontend/src/utils/adminDomains.ts frontend/tests/adminDomains.test.ts
git commit -m "feat(admin): validate domain binding must be a public space"
```

---

## 验收对照（PRD §10）

| PRD 验收项 | 覆盖任务 |
|---|---|
| code 一致 → 放行 | Task 2/3 |
| code 不一致 → 阻断 DOMAIN_MISMATCH | Task 2/3 |
| 编码缺失/非法 → FILE_CODE_MISSING | Task 1/2/3 |
| 空间无业务域 → SPACE_DOMAIN_UNCONFIGURED | Task 2/3 |
| 多业务域绑定命中任一放行 | Task 2 |
| 绑定空间下拉仅公共空间 | Task 4 |
| 知识空间配置栏保持现状 | 不改动（无任务，符合预期） |
| 「调毕昇发布」与门户发布入口 | 不在本计划（PRD §11 开放项） |

## Self-Review 结论

- **Spec 覆盖**：PRD §3 校验规则、§5 边界、§6 绑定候选、§8 接口、§9 文案均有对应任务；§7（知识空间栏不动）无需任务；§11 开放项明确排除。
- **占位符**：无 TBD/TODO，所有步骤含完整代码与命令。
- **类型一致**：`PublishPrecheckResult` / `DomainRef` / `parse_domain_code` / `getPublicSpaceOptions` 在定义与消费处签名一致；`reason_code` 取值跨任务一致。
