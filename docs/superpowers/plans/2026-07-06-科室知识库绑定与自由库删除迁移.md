# 科室知识库绑定 与 自由库删除迁移 — 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让团队知识库可在 portal 后台绑定部门变「科室库」，未绑定为「自由库」；删除自由库时自动把数据迁移到「创建者所属部门对应的科室库」再删除。

**Architecture:** 复用 bisheng 既有 `department_knowledge_space` 绑定表，不新增表。bisheng backend 提供绑定/解绑 admin API，并在 `delete_space` 前置挂钩里判定自由库→异步 Celery 迁移(复用 `copy_normal`/`copy_vector`)→迁移成功后删源库。portal 只做 BFF 代理 + 后台绑定 UI。

**Tech Stack:** Python / FastAPI / SQLModel / Celery / Milvus / Elasticsearch / MinIO（bisheng）；FastAPI BFF + Vite/React 19 + TypeScript（portal）。

## Global Constraints

- **两个 worktree（全部用绝对路径操作）**：
  - bisheng backend：`/Users/zhangguoqing/works/bisheng_2-dept-knowledge-binding`（分支 `feat/dept-knowledge-binding`，基于 `origin/feat/2.5.0-sg`）
  - portal：`/Users/zhangguoqing/works/shougang-dept-knowledge-binding`（分支 `feat/dept-knowledge-binding`，基于 `origin/master`）
- **迁移锁用 `KnowledgeState.COPYING=2`**，不新增数据库字段、不写 alembic（方案 b，零 schema 变更）。
- **R3 前提**：portal BFF 用「后台数据源配置里那个 bisheng 账号」的系统级 token 调 admin API，**该账号必须是 super_admin**。此为部署前提，需在 spec/README 注明。
- **bisheng 测试**：无 `.venv`，从 `src/backend` 目录用 `uv run pytest ...`（`asyncio_mode=auto`，async 测试无需 marker）。
- **portal BFF 测试**：`cd backend && pytest tests/...`；**portal 前端测试**：`cd frontend && npm test`（先 `tsc` 再 `node --test`，只测 `src/utils/` 纯函数）。
- **不做**（YAGNI）：绑定不改任何访问权限；embedding 不一致不重嵌入(改阻断)；不支持 1 部门多库/1 库多部门；不动 `batch_create_spaces`。
- 每个 task 完成即 commit；提交信息用中文，遵循 `feat(scope):` / `test(scope):` 约定。

---

## Phase 1 — bisheng_2 backend

> 全部在 worktree `/Users/zhangguoqing/works/bisheng_2-dept-knowledge-binding` 内。命令默认 `cd /Users/zhangguoqing/works/bisheng_2-dept-knowledge-binding/src/backend`。

### Task 1: 绑定表 DAO —— 新增解绑方法

**Files:**
- Modify: `src/backend/bisheng/knowledge/domain/models/department_knowledge_space.py`（`DepartmentKnowledgeSpaceDao`，class 从行 84）
- Test: `src/backend/test/test_department_knowledge_space_dao_unbind.py`（新建）

**Interfaces:**
- Produces:
  - `DepartmentKnowledgeSpaceDao.adelete_by_space_id(space_id: int) -> bool`（删除该空间的绑定行，删到返回 True，无绑定返回 False）
- Consumes: 既有 `aget_by_space_id(space_id) -> Optional[DepartmentKnowledgeSpace]`（行 150）

- [ ] **Step 1: 写失败测试**

创建 `src/backend/test/test_department_knowledge_space_dao_unbind.py`：

```python
import pytest
from bisheng.knowledge.domain.models.department_knowledge_space import (
    DepartmentKnowledgeSpaceDao,
)


@pytest.mark.asyncio
async def test_adelete_by_space_id_removes_binding():
    await DepartmentKnowledgeSpaceDao.acreate(
        tenant_id=1, department_id=11, space_id=101, created_by=1,
    )
    assert await DepartmentKnowledgeSpaceDao.aget_by_space_id(101) is not None

    deleted = await DepartmentKnowledgeSpaceDao.adelete_by_space_id(101)
    assert deleted is True
    assert await DepartmentKnowledgeSpaceDao.aget_by_space_id(101) is None


@pytest.mark.asyncio
async def test_adelete_by_space_id_missing_returns_false():
    assert await DepartmentKnowledgeSpaceDao.adelete_by_space_id(999999) is False
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd /Users/zhangguoqing/works/bisheng_2-dept-knowledge-binding/src/backend && uv run pytest test/test_department_knowledge_space_dao_unbind.py -v`
Expected: FAIL —— `AttributeError: ... has no attribute 'adelete_by_space_id'`

- [ ] **Step 3: 实现 `adelete_by_space_id`**

在 `DepartmentKnowledgeSpaceDao` 里（`aget_department_ids_by_space_ids` 之后）追加：

```python
    @classmethod
    async def adelete_by_space_id(cls, space_id: int) -> bool:
        async with get_async_db_session() as session:
            result = await session.exec(
                select(DepartmentKnowledgeSpace).where(
                    DepartmentKnowledgeSpace.space_id == space_id,
                )
            )
            row = result.first()
            if row is None:
                return False
            await session.delete(row)
            await session.commit()
            return True
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd /Users/zhangguoqing/works/bisheng_2-dept-knowledge-binding/src/backend && uv run pytest test/test_department_knowledge_space_dao_unbind.py -v`
Expected: PASS（2 passed）

- [ ] **Step 5: 提交**

```bash
cd /Users/zhangguoqing/works/bisheng_2-dept-knowledge-binding
git add src/backend/bisheng/knowledge/domain/models/department_knowledge_space.py src/backend/test/test_department_knowledge_space_dao_unbind.py
git commit -m "feat(knowledge): 绑定表 DAO 新增 adelete_by_space_id 解绑方法"
```

---

### Task 2: 迁移目标查找 —— 沿部门 path 向上找最近科室库

**Files:**
- Create: `src/backend/bisheng/knowledge/domain/services/free_space_migration_service.py`
- Test: `src/backend/test/test_free_space_migration_target.py`

**Interfaces:**
- Consumes:
  - `UserDepartmentDao.aget_user_primary_department(user_id) -> Optional[UserDepartment]`（`database/models/department.py:1236`，取 `.department_id`）
  - `DepartmentDao.aget_by_id(dept_id)` 及 `Department.path`（格式 `/1/2/3/`）
  - `DepartmentKnowledgeSpaceDao.aget_space_id_by_department_id(dept_id) -> Optional[int]`（行 176）
- Produces:
  - `FreeSpaceMigrationService.resolve_target_department_space(creator_user_id: int) -> Optional[int]`
    返回最近的已绑定科室库 `space_id`；创建者无主部门 → None；主部门及祖先都无绑定 → None。
  - 辅助：`FreeSpaceMigrationService._department_id_chain(primary_department_id: int) -> list[int]`
    返回 `[主部门id, 父, 祖父, ... 根]`（含自身，自近到远）。

- [ ] **Step 1: 写失败测试**

创建 `src/backend/test/test_free_space_migration_target.py`：

```python
import pytest
from unittest.mock import AsyncMock, patch

from bisheng.knowledge.domain.services.free_space_migration_service import (
    FreeSpaceMigrationService,
)


class _Dept:
    def __init__(self, id, path):
        self.id = id
        self.path = path


class _UserDept:
    def __init__(self, department_id):
        self.department_id = department_id


@pytest.mark.asyncio
async def test_no_primary_department_returns_none():
    with patch(
        "bisheng.knowledge.domain.services.free_space_migration_service.UserDepartmentDao.aget_user_primary_department",
        new=AsyncMock(return_value=None),
    ):
        assert await FreeSpaceMigrationService.resolve_target_department_space(7) is None


@pytest.mark.asyncio
async def test_primary_department_bound_hits_directly():
    with patch(
        "bisheng.knowledge.domain.services.free_space_migration_service.UserDepartmentDao.aget_user_primary_department",
        new=AsyncMock(return_value=_UserDept(3)),
    ), patch(
        "bisheng.knowledge.domain.services.free_space_migration_service.DepartmentDao.aget_by_id",
        new=AsyncMock(return_value=_Dept(3, "/1/2/3/")),
    ), patch(
        "bisheng.knowledge.domain.services.free_space_migration_service.DepartmentKnowledgeSpaceDao.aget_space_id_by_department_id",
        new=AsyncMock(side_effect=lambda d: 500 if d == 3 else None),
    ):
        assert await FreeSpaceMigrationService.resolve_target_department_space(7) == 500


@pytest.mark.asyncio
async def test_walks_up_to_nearest_ancestor_binding():
    # 主部门 3 未绑定，父 2 未绑定，祖 1 绑定 → 命中最近的已绑定祖先(1)
    with patch(
        "bisheng.knowledge.domain.services.free_space_migration_service.UserDepartmentDao.aget_user_primary_department",
        new=AsyncMock(return_value=_UserDept(3)),
    ), patch(
        "bisheng.knowledge.domain.services.free_space_migration_service.DepartmentDao.aget_by_id",
        new=AsyncMock(return_value=_Dept(3, "/1/2/3/")),
    ), patch(
        "bisheng.knowledge.domain.services.free_space_migration_service.DepartmentKnowledgeSpaceDao.aget_space_id_by_department_id",
        new=AsyncMock(side_effect=lambda d: 900 if d == 1 else None),
    ):
        assert await FreeSpaceMigrationService.resolve_target_department_space(7) == 900


@pytest.mark.asyncio
async def test_no_binding_anywhere_returns_none():
    with patch(
        "bisheng.knowledge.domain.services.free_space_migration_service.UserDepartmentDao.aget_user_primary_department",
        new=AsyncMock(return_value=_UserDept(3)),
    ), patch(
        "bisheng.knowledge.domain.services.free_space_migration_service.DepartmentDao.aget_by_id",
        new=AsyncMock(return_value=_Dept(3, "/1/2/3/")),
    ), patch(
        "bisheng.knowledge.domain.services.free_space_migration_service.DepartmentKnowledgeSpaceDao.aget_space_id_by_department_id",
        new=AsyncMock(return_value=None),
    ):
        assert await FreeSpaceMigrationService.resolve_target_department_space(7) is None
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd /Users/zhangguoqing/works/bisheng_2-dept-knowledge-binding/src/backend && uv run pytest test/test_free_space_migration_target.py -v`
Expected: FAIL —— `ModuleNotFoundError: ... free_space_migration_service`

- [ ] **Step 3: 实现 service 的目标查找**

创建 `src/backend/bisheng/knowledge/domain/services/free_space_migration_service.py`：

```python
from __future__ import annotations

import logging
from typing import List, Optional

from bisheng.database.models.department import DepartmentDao, UserDepartmentDao
from bisheng.knowledge.domain.models.department_knowledge_space import (
    DepartmentKnowledgeSpaceDao,
)

_logger = logging.getLogger(__name__)


class FreeSpaceMigrationService:
    """自由知识库删除时的迁移目标查找与删除前置判定。"""

    @staticmethod
    def _parse_path_ids(path: Optional[str]) -> List[int]:
        """把物化路径 '/1/2/3/' 解析成 [1, 2, 3]（从根到叶）。"""
        if not path:
            return []
        ids: List[int] = []
        for part in path.split("/"):
            part = part.strip()
            if part.isdigit():
                ids.append(int(part))
        return ids

    @classmethod
    async def _department_id_chain(cls, primary_department_id: int) -> List[int]:
        """返回 [主部门, 父, 祖父, ... 根]（自近到远，含自身）。"""
        dept = await DepartmentDao.aget_by_id(primary_department_id)
        if dept is None:
            return [primary_department_id]
        path_ids = cls._parse_path_ids(getattr(dept, "path", None))
        if primary_department_id not in path_ids:
            path_ids.append(primary_department_id)
        # path 是 根→叶；反转成 叶→根（自近到远）
        return list(reversed(path_ids))

    @classmethod
    async def resolve_target_department_space(cls, creator_user_id: int) -> Optional[int]:
        """创建者主部门→沿 path 向上找最近的已绑定科室库 space_id；找不到返回 None。"""
        primary = await UserDepartmentDao.aget_user_primary_department(creator_user_id)
        if primary is None or getattr(primary, "department_id", None) is None:
            return None
        for dept_id in await cls._department_id_chain(int(primary.department_id)):
            space_id = await DepartmentKnowledgeSpaceDao.aget_space_id_by_department_id(dept_id)
            if space_id is not None:
                return int(space_id)
        return None
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd /Users/zhangguoqing/works/bisheng_2-dept-knowledge-binding/src/backend && uv run pytest test/test_free_space_migration_target.py -v`
Expected: PASS（4 passed）

- [ ] **Step 5: 提交**

```bash
cd /Users/zhangguoqing/works/bisheng_2-dept-knowledge-binding
git add src/backend/bisheng/knowledge/domain/services/free_space_migration_service.py src/backend/test/test_free_space_migration_target.py
git commit -m "feat(knowledge): 自由库迁移目标查找(沿部门path向上找最近科室库)"
```

---

### Task 3: 删除前置判定 `pre_delete_guard` + 新增 errcode

**Files:**
- Modify: `src/backend/bisheng/knowledge/domain/services/free_space_migration_service.py`（加判定）
- Modify: `src/backend/bisheng/common/errcode/knowledge_space.py`（加 errcode；先确认文件路径/风格）
- Test: `src/backend/test/test_free_space_migration_guard.py`

**Interfaces:**
- Consumes:
  - `DepartmentKnowledgeSpaceDao.aget_by_space_id(space_id)`（判是否科室库）
  - `KnowledgeSpaceScopeDao.aget_by_space_id(space_id) -> Optional[KnowledgeSpaceScope]`（`models/knowledge_space_scope.py:113`，取 `.level`）
  - `resolve_target_department_space`（Task 2）
  - `KnowledgeDao.aquery_by_id(id)`（取 target 的 `.model` embedding 模型）
- Produces:
  - `MigrationDecision`（`@dataclass`）：`action: Literal["normal_delete","migrate","block"]`、`target_space_id: Optional[int]`、`reason: str`
  - `FreeSpaceMigrationService.pre_delete_guard(space) -> MigrationDecision`
  - errcode：`DepartmentSpaceDeleteForbiddenError`、`FreeSpaceMigrationTargetNotFoundError`、`FreeSpaceMigrationEmbeddingMismatchError`、`FreeSpaceMigratingError`

判定规则（`space` 为已加载的 `Knowledge`）：
1. `space.state == KnowledgeState.COPYING.value` → `block`，reason=正在迁移
2. 该空间已绑定部门(`aget_by_space_id` 非 None) → `block`，reason=科室库禁止删除请先解绑
3. scope.level != `'team'`（个人/公共/部门等） → `normal_delete`
4. 自由 team 库：
   - 创建者主部门链无绑定科室库 → `block`，reason=无法迁移(创建者无主部门/部门树无科室库)
   - `source.model != target.model` → `block`，reason=embedding 模型不一致
   - 否则 → `migrate`，`target_space_id=...`

- [ ] **Step 1: 确认 errcode 文件风格**

Run: `cd /Users/zhangguoqing/works/bisheng_2-dept-knowledge-binding/src/backend && sed -n '1,60p' bisheng/common/errcode/knowledge_space.py`
记录：基类（应为 `BaseErrorCode`）、code 号段、message 写法。若该文件不存在，改用 `grep -rn "DepartmentKnowledgeSpaceExistsError" bisheng/common/errcode/` 找到实际文件。

- [ ] **Step 2: 写失败测试**

创建 `src/backend/test/test_free_space_migration_guard.py`：

```python
import pytest
from unittest.mock import AsyncMock, patch

from bisheng.knowledge.domain.models.knowledge import KnowledgeState
from bisheng.knowledge.domain.services.free_space_migration_service import (
    FreeSpaceMigrationService,
)

MOD = "bisheng.knowledge.domain.services.free_space_migration_service"


class _Space:
    def __init__(self, id, user_id, state=KnowledgeState.PUBLISHED.value, model="emb-1"):
        self.id = id
        self.user_id = user_id
        self.state = state
        self.model = model


class _Scope:
    def __init__(self, level):
        self.level = level


def _patches(*, binding=None, scope_level="team", target_id=None, target_model="emb-1"):
    return [
        patch(f"{MOD}.DepartmentKnowledgeSpaceDao.aget_by_space_id", new=AsyncMock(return_value=binding)),
        patch(f"{MOD}.KnowledgeSpaceScopeDao.aget_by_space_id", new=AsyncMock(return_value=_Scope(scope_level))),
        patch.object(FreeSpaceMigrationService, "resolve_target_department_space", new=AsyncMock(return_value=target_id)),
        patch(f"{MOD}.KnowledgeDao.aquery_by_id", new=AsyncMock(return_value=_Space(target_id, 0, model=target_model) if target_id else None)),
    ]


async def _run(space, **kw):
    ps = _patches(**kw)
    for p in ps:
        p.start()
    try:
        return await FreeSpaceMigrationService.pre_delete_guard(space)
    finally:
        for p in ps:
            p.stop()


@pytest.mark.asyncio
async def test_migrating_state_blocks():
    d = await _run(_Space(1, 5, state=KnowledgeState.COPYING.value))
    assert d.action == "block"


@pytest.mark.asyncio
async def test_bound_department_space_blocks():
    d = await _run(_Space(1, 5), binding=object())
    assert d.action == "block"


@pytest.mark.asyncio
async def test_non_team_space_normal_delete():
    d = await _run(_Space(1, 5), scope_level="personal")
    assert d.action == "normal_delete"


@pytest.mark.asyncio
async def test_free_space_no_target_blocks():
    d = await _run(_Space(1, 5), target_id=None)
    assert d.action == "block"


@pytest.mark.asyncio
async def test_free_space_embedding_mismatch_blocks():
    d = await _run(_Space(1, 5, model="emb-1"), target_id=900, target_model="emb-2")
    assert d.action == "block"


@pytest.mark.asyncio
async def test_free_space_ok_migrates():
    d = await _run(_Space(1, 5, model="emb-1"), target_id=900, target_model="emb-1")
    assert d.action == "migrate"
    assert d.target_space_id == 900
```

- [ ] **Step 3: 跑测试确认失败**

Run: `cd /Users/zhangguoqing/works/bisheng_2-dept-knowledge-binding/src/backend && uv run pytest test/test_free_space_migration_guard.py -v`
Expected: FAIL —— `AttributeError: ... 'pre_delete_guard'`

- [ ] **Step 4: 加 errcode**

在 Step 1 找到的 errcode 文件里追加（code 号段按该文件现有规律取空闲值，示例用 108xx）：

```python
class DepartmentSpaceDeleteForbiddenError(BaseErrorCode):
    Code: int = 10810
    Msg: str = '科室知识库禁止删除，请先在后台解除部门绑定'


class FreeSpaceMigrationTargetNotFoundError(BaseErrorCode):
    Code: int = 10811
    Msg: str = '无法迁移：创建者无主部门，或所属部门（含上级）未绑定科室知识库'


class FreeSpaceMigrationEmbeddingMismatchError(BaseErrorCode):
    Code: int = 10812
    Msg: str = '无法迁移：源库与目标科室库的向量模型不一致'


class FreeSpaceMigratingError(BaseErrorCode):
    Code: int = 10813
    Msg: str = '该知识库正在迁移中，请稍后再试'
```

- [ ] **Step 5: 实现 `pre_delete_guard` + `MigrationDecision`**

在 `free_space_migration_service.py` 顶部补 import 与 dataclass，并加方法：

```python
from dataclasses import dataclass
from typing import Literal

from bisheng.knowledge.domain.models.knowledge import KnowledgeDao, KnowledgeState
from bisheng.knowledge.domain.models.knowledge_space_scope import (
    KnowledgeSpaceLevelEnum,
    KnowledgeSpaceScopeDao,
)


@dataclass
class MigrationDecision:
    action: Literal["normal_delete", "migrate", "block"]
    target_space_id: Optional[int] = None
    reason: str = ""
```

```python
    @classmethod
    async def pre_delete_guard(cls, space) -> "MigrationDecision":
        if space.state == KnowledgeState.COPYING.value:
            return MigrationDecision("block", reason="migrating")
        binding = await DepartmentKnowledgeSpaceDao.aget_by_space_id(space.id)
        if binding is not None:
            return MigrationDecision("block", reason="department_space_forbidden")
        scope = await KnowledgeSpaceScopeDao.aget_by_space_id(space.id)
        level = getattr(scope, "level", None)
        if level != KnowledgeSpaceLevelEnum.TEAM.value and level != KnowledgeSpaceLevelEnum.TEAM:
            return MigrationDecision("normal_delete")
        target_id = await cls.resolve_target_department_space(space.user_id)
        if target_id is None:
            return MigrationDecision("block", reason="target_not_found")
        target = await KnowledgeDao.aquery_by_id(target_id)
        if target is None or target.model != space.model:
            return MigrationDecision("block", reason="embedding_mismatch")
        return MigrationDecision("migrate", target_space_id=target_id)
```

> 注：`KnowledgeSpaceLevelEnum` 是 `str` Enum，DB 存字符串 `'team'`；上面同时比较 `.value` 与成员以兼容两种取值。

- [ ] **Step 6: 跑测试确认通过**

Run: `cd /Users/zhangguoqing/works/bisheng_2-dept-knowledge-binding/src/backend && uv run pytest test/test_free_space_migration_guard.py -v`
Expected: PASS（6 passed）

- [ ] **Step 7: 提交**

```bash
cd /Users/zhangguoqing/works/bisheng_2-dept-knowledge-binding
git add -A
git commit -m "feat(knowledge): 自由库删除前置判定 pre_delete_guard + 迁移相关 errcode"
```

---

### Task 4: 空间感知的异步迁移任务 `space_migrate_celery`

**Files:**
- Create: `src/backend/bisheng/worker/knowledge/space_migrate_worker.py`
- Modify: `src/backend/bisheng/worker/__init__.py`（注册任务，照 `init_knowledge_space_indices` 的 import 方式，行 11 附近）
- Test: `src/backend/test/test_space_migrate_worker.py`

**Interfaces:**
- Consumes:
  - `KnowledgeDao.query_by_id(id)`（sync）、`KnowledgeDao.update_state(knowledge_id, state, update_time=None)`（sync，行 192）
  - `KnowledgeFileDao.get_file_by_filters(knowledge_id, page, page_size)`（sync，行 550）、`get_file_by_condition(knowledge_id)`（sync 全量取 md5，行 400）
  - `copy_normal(one, source_knowledge, target_knowledge, op_user_id)`（file_worker.py:91）
  - `KnowledgeSpaceService(request, login_user).delete_space(space_id, migrate_free_space=False)`（Task 5 产出）
  - `UserPayload(user_id=, user_name=, role=)`（先例 `worker/information/article.py:437`）
- Produces:
  - `space_migrate_celery(param: dict) -> str`（param: `source_id`, `target_id`, `op_user_id`）
  - 内部 `_migrate_and_delete_source(source_id, target_id, op_user_id)`（async）

- [ ] **Step 1: 写失败测试**（用 mock 隔离 milvus/es/minio；只验证编排：去重、逐文件 copy、成功后删源库、失败回滚）

创建 `src/backend/test/test_space_migrate_worker.py`：

```python
from types import SimpleNamespace
from unittest.mock import MagicMock, patch

MOD = "bisheng.worker.knowledge.space_migrate_worker"


def _kfile(id, md5, status=2):
    return SimpleNamespace(id=id, md5=md5, status=status)


def test_migrate_skips_duplicate_md5_and_copies_rest():
    source = SimpleNamespace(id=1, state=1, update_time=None, model="e")
    target = SimpleNamespace(id=2, state=1, update_time=None, model="e")
    pages = [[_kfile(10, "a"), _kfile(11, "b")], []]

    with patch(f"{MOD}.KnowledgeDao.query_by_id", side_effect=lambda i: source if i == 1 else target), \
         patch(f"{MOD}.KnowledgeDao.update_state"), \
         patch(f"{MOD}.KnowledgeFileDao.get_file_by_condition", return_value=[_kfile(99, "a")]), \
         patch(f"{MOD}.KnowledgeFileDao.get_file_by_filters", side_effect=lambda kid, page, page_size: pages[page - 1]), \
         patch(f"{MOD}.copy_normal") as copy_normal, \
         patch(f"{MOD}._delete_source_space") as del_src:
        from bisheng.worker.knowledge import space_migrate_worker as m
        m.space_migrate_celery({"source_id": 1, "target_id": 2, "op_user_id": 5})

        copied_ids = [c.args[0].id for c in copy_normal.call_args_list]
        assert copied_ids == [11]          # md5 "a" 已存在于目标，跳过 id=10
        del_src.assert_called_once()        # 成功后删源库


def test_migrate_failure_rolls_back_state_and_keeps_source():
    source = SimpleNamespace(id=1, state=1, update_time=None, model="e")
    target = SimpleNamespace(id=2, state=1, update_time=None, model="e")

    with patch(f"{MOD}.KnowledgeDao.query_by_id", side_effect=lambda i: source if i == 1 else target), \
         patch(f"{MOD}.KnowledgeDao.update_state") as update_state, \
         patch(f"{MOD}.KnowledgeFileDao.get_file_by_condition", return_value=[]), \
         patch(f"{MOD}.KnowledgeFileDao.get_file_by_filters", side_effect=RuntimeError("boom")), \
         patch(f"{MOD}._delete_source_space") as del_src:
        from bisheng.worker.knowledge import space_migrate_worker as m
        m.space_migrate_celery({"source_id": 1, "target_id": 2, "op_user_id": 5})

        del_src.assert_not_called()         # 失败不删源库
        # 最后一次 update_state 把源库恢复为 PUBLISHED（回滚迁移中）
        last = update_state.call_args_list[-1]
        from bisheng.knowledge.domain.models.knowledge import KnowledgeState
        assert last.kwargs.get("state") == KnowledgeState.PUBLISHED or last.args[1:] and last.args[1] == KnowledgeState.PUBLISHED
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd /Users/zhangguoqing/works/bisheng_2-dept-knowledge-binding/src/backend && uv run pytest test/test_space_migrate_worker.py -v`
Expected: FAIL —— `ModuleNotFoundError: ... space_migrate_worker`

- [ ] **Step 3: 实现迁移任务**

创建 `src/backend/bisheng/worker/knowledge/space_migrate_worker.py`：

```python
import asyncio
import logging
from fastapi import Request

from bisheng.common.dependencies.user_deps import UserPayload
from bisheng.knowledge.domain.models.knowledge import KnowledgeDao, KnowledgeState
from bisheng.knowledge.domain.models.knowledge_file import KnowledgeFileDao
from bisheng.worker.knowledge.file_worker import copy_normal
from bisheng.worker.main import bisheng_celery

_logger = logging.getLogger(__name__)
_PAGE_SIZE = 20


async def _delete_source_space(source_id: int, op_user_id: int) -> None:
    """迁移成功后删源库；传 migrate_free_space=False 跳过前置判定避免死循环。"""
    from bisheng.knowledge.domain.services.knowledge_space_service import (
        KnowledgeSpaceService,
    )
    login_user = UserPayload(user_id=op_user_id, user_name="system-worker", role="user")
    request = Request(scope={"type": "http"})
    svc = KnowledgeSpaceService(request=request, login_user=login_user)
    await svc.delete_space(source_id, migrate_free_space=False)


def _do_migrate(source_id: int, target_id: int, op_user_id: int) -> None:
    source = KnowledgeDao.query_by_id(source_id)
    target = KnowledgeDao.query_by_id(target_id)
    existing = KnowledgeFileDao.get_file_by_condition(target_id) or []
    target_md5 = {f.md5 for f in existing if getattr(f, "md5", None)}

    page = 1
    while True:
        files = KnowledgeFileDao.get_file_by_filters(source_id, page=page, page_size=_PAGE_SIZE)
        if not files:
            break
        for one in files:
            if one.md5 and one.md5 in target_md5:
                continue
            copy_normal(one, source, target, op_user_id)
            if one.md5:
                target_md5.add(one.md5)
        if len(files) < _PAGE_SIZE:
            break
        page += 1


@bisheng_celery.task(acks_late=True)
def space_migrate_celery(param: dict) -> str:
    source_id = param["source_id"]
    target_id = param["target_id"]
    op_user_id = param["op_user_id"]
    _logger.info("space_migrate start source=%s target=%s", source_id, target_id)
    try:
        _do_migrate(source_id, target_id, op_user_id)
        asyncio.run(_delete_source_space(source_id, op_user_id))
        _logger.info("space_migrate done source=%s", source_id)
        return "space migrate done"
    except Exception as e:  # 回滚：源库状态恢复，保留源库
        _logger.exception("space_migrate failed source=%s: %s", source_id, e)
        KnowledgeDao.update_state(knowledge_id=source_id, state=KnowledgeState.PUBLISHED)
        return "space migrate failed"
```

- [ ] **Step 4: 注册任务**

在 `src/backend/bisheng/worker/__init__.py` 的 knowledge worker import 区块（`init_knowledge_space_indices` 附近，行 11）加：

```python
    space_migrate_celery,
```
并确认它从 `bisheng.worker.knowledge.space_migrate_worker` 导入（若 `__init__` 用显式模块路径，照 `space_init_worker` 的写法加一行 import）。

- [ ] **Step 5: 跑测试确认通过**

Run: `cd /Users/zhangguoqing/works/bisheng_2-dept-knowledge-binding/src/backend && uv run pytest test/test_space_migrate_worker.py -v`
Expected: PASS（2 passed）

- [ ] **Step 6: 提交**

```bash
cd /Users/zhangguoqing/works/bisheng_2-dept-knowledge-binding
git add -A
git commit -m "feat(worker): 空间感知的自由库→科室库异步迁移任务 space_migrate_celery"
```

---

### Task 5: `delete_space` 前置挂钩 + `migrate_free_space` 参数

**Files:**
- Modify: `src/backend/bisheng/knowledge/domain/services/knowledge_space_service.py`（`delete_space`，行 5215）
- Test: `src/backend/test/test_delete_space_migration_hook.py`

**Interfaces:**
- Consumes: `FreeSpaceMigrationService.pre_delete_guard`（Task 3）、`space_migrate_celery.delay`（Task 4）、errcode（Task 3）
- Produces: `delete_space(self, space_id, *, migrate_free_space: bool = True) -> None`
  - `migrate_free_space=True`（默认，外部入口）：先跑 guard；`block`→抛对应 errcode；`migrate`→置源库 COPYING、投递迁移任务、直接 return；`normal_delete`→继续原清理。
  - `migrate_free_space=False`（迁移任务回调）：跳过 guard，直接原清理。

- [ ] **Step 1: 写失败测试**（mock 掉重清理，只验证分支路由）

创建 `src/backend/test/test_delete_space_migration_hook.py`：

```python
import pytest
from types import SimpleNamespace
from unittest.mock import AsyncMock, MagicMock, patch

from bisheng.knowledge.domain.models.knowledge import KnowledgeState, KnowledgeTypeEnum
from bisheng.knowledge.domain.services.knowledge_space_service import KnowledgeSpaceService
from bisheng.knowledge.domain.services.free_space_migration_service import MigrationDecision

MOD = "bisheng.knowledge.domain.services.knowledge_space_service"


def _svc():
    svc = KnowledgeSpaceService.__new__(KnowledgeSpaceService)
    svc.login_user = SimpleNamespace(user_id=5)
    svc.request = None
    svc._require_permission_id = AsyncMock()
    svc._list_space_child_resources = AsyncMock(return_value=[])
    return svc


def _space():
    return SimpleNamespace(id=1, type=KnowledgeTypeEnum.SPACE.value, state=1,
                           is_favorite=False, name="s", user_id=5, model="e")


@pytest.mark.asyncio
async def test_migrate_branch_enqueues_and_returns_without_delete():
    svc = _svc()
    with patch(f"{MOD}.KnowledgeDao.aquery_by_id", new=AsyncMock(return_value=_space())), \
         patch(f"{MOD}.FreeSpaceMigrationService.pre_delete_guard",
               new=AsyncMock(return_value=MigrationDecision("migrate", target_space_id=900))), \
         patch(f"{MOD}.KnowledgeDao.async_update_state", new=AsyncMock()) as upd, \
         patch(f"{MOD}.space_migrate_celery") as task, \
         patch(f"{MOD}.KnowledgeService.delete_knowledge_file_in_vector") as del_vec:
        await svc.delete_space(1)
        task.delay.assert_called_once()
        del_vec.assert_not_called()        # 没有进入真正清理


@pytest.mark.asyncio
async def test_block_branch_raises():
    from bisheng.common.errcode.knowledge_space import DepartmentSpaceDeleteForbiddenError
    svc = _svc()
    with patch(f"{MOD}.KnowledgeDao.aquery_by_id", new=AsyncMock(return_value=_space())), \
         patch(f"{MOD}.FreeSpaceMigrationService.pre_delete_guard",
               new=AsyncMock(return_value=MigrationDecision("block", reason="department_space_forbidden"))):
        with pytest.raises(Exception):
            await svc.delete_space(1)


@pytest.mark.asyncio
async def test_migrate_free_space_false_skips_guard():
    svc = _svc()
    guard = AsyncMock()
    with patch(f"{MOD}.KnowledgeDao.aquery_by_id", new=AsyncMock(return_value=_space())), \
         patch(f"{MOD}.FreeSpaceMigrationService.pre_delete_guard", new=guard), \
         patch(f"{MOD}.KnowledgeService.delete_knowledge_file_in_vector"), \
         patch(f"{MOD}.KnowledgeService.delete_knowledge_file_in_minio"), \
         patch(f"{MOD}.KnowledgeDao.async_delete_knowledge", new=AsyncMock()), \
         patch(f"{MOD}.KnowledgeSpaceContentStat.enqueue_space_delete_stat_async", new=AsyncMock()), \
         patch.object(svc, "_send_space_event_notification", new=AsyncMock()), \
         patch(f"{MOD}.SpaceChannelMemberDao.async_get_members_by_space", new=AsyncMock(return_value=[])), \
         patch.object(svc, "_cleanup_resource_tuples", new=AsyncMock()), \
         patch(f"{MOD}.SpaceChannelMemberDao.clean_space_member", new=AsyncMock()), \
         patch(f"{MOD}.KnowledgeSpaceTagLibraryDao.adelete_private_for_knowledge", new=AsyncMock()), \
         patch(f"{MOD}.KnowledgeAuditTelemetryService.audit_delete_knowledge_space", new=AsyncMock()), \
         patch(f"{MOD}.KnowledgeAuditTelemetryService.telemetry_delete_knowledge"):
        await svc.delete_space(1, migrate_free_space=False)
        guard.assert_not_called()          # 跳过判定
```

> 若某些 patch 目标名与实际 import 不符，按 `knowledge_space_service.py` 顶部实际 import 名调整。运行时报 `AttributeError` 即提示要改的符号名。

- [ ] **Step 2: 跑测试确认失败**

Run: `cd /Users/zhangguoqing/works/bisheng_2-dept-knowledge-binding/src/backend && uv run pytest test/test_delete_space_migration_hook.py -v`
Expected: FAIL —— `TypeError: delete_space() got an unexpected keyword argument 'migrate_free_space'`（以及 guard 未挂钩）

- [ ] **Step 3: 加 import + 改签名 + 插入分支**

在 `knowledge_space_service.py` 顶部 import 区加：

```python
from bisheng.knowledge.domain.services.free_space_migration_service import FreeSpaceMigrationService
from bisheng.worker.knowledge.space_migrate_worker import space_migrate_celery
from bisheng.common.errcode.knowledge_space import (
    DepartmentSpaceDeleteForbiddenError,
    FreeSpaceMigrationTargetNotFoundError,
    FreeSpaceMigrationEmbeddingMismatchError,
    FreeSpaceMigratingError,
)
```

> 若出现循环 import（space_migrate_worker → knowledge_space_service），把 `space_migrate_celery` 改为函数内延迟 import（在投递处 `from ... import space_migrate_celery`）。

把 `delete_space` 签名（行 5215）改为：

```python
    async def delete_space(self, space_id: int, *, migrate_free_space: bool = True) -> None:
```

在权限校验（原行 5221 `_require_permission_id` 之后）与 `child_resources = ...`（原 5222）之间插入：

```python
        if migrate_free_space:
            decision = await FreeSpaceMigrationService.pre_delete_guard(space)
            if decision.action == "block":
                raise {
                    "department_space_forbidden": DepartmentSpaceDeleteForbiddenError,
                    "target_not_found": FreeSpaceMigrationTargetNotFoundError,
                    "embedding_mismatch": FreeSpaceMigrationEmbeddingMismatchError,
                    "migrating": FreeSpaceMigratingError,
                }.get(decision.reason, DepartmentSpaceDeleteForbiddenError)()
            if decision.action == "migrate":
                await KnowledgeDao.async_update_state(
                    knowledge_id=space_id, state=KnowledgeState.COPYING,
                )
                space_migrate_celery.delay({
                    "source_id": space_id,
                    "target_id": decision.target_space_id,
                    "op_user_id": self.login_user.user_id,
                })
                return
            # decision.action == "normal_delete" → 继续原清理逻辑
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd /Users/zhangguoqing/works/bisheng_2-dept-knowledge-binding/src/backend && uv run pytest test/test_delete_space_migration_hook.py -v`
Expected: PASS（3 passed）

- [ ] **Step 5: 回归既有删除测试**

Run: `cd /Users/zhangguoqing/works/bisheng_2-dept-knowledge-binding/src/backend && uv run pytest test/ -k "delete_space or knowledge_space" -q`
Expected: 既有相关测试仍 PASS（若有失败，检查是否 normal_delete 路径被破坏）

- [ ] **Step 6: 提交**

```bash
cd /Users/zhangguoqing/works/bisheng_2-dept-knowledge-binding
git add -A
git commit -m "feat(knowledge): delete_space 前置挂钩自由库迁移(migrate_free_space 参数)"
```

---

### Task 6: 绑定管理 admin API（service 方法 + endpoints）

**Files:**
- Modify: `src/backend/bisheng/knowledge/domain/services/department_knowledge_space_service.py`（加 bind/unbind/list/bindable/departments）
- Modify: `src/backend/bisheng/knowledge/api/endpoints/knowledge_space.py`（加 5 个 endpoint，前缀 `/knowledge/space`，照行 283-314 现有 department endpoint 风格）
- Test: `src/backend/test/test_department_binding_admin.py`

**Interfaces:**
- Consumes: `_ensure_super_admin`（行 47）、`DepartmentKnowledgeSpaceDao.acreate`/`aget_by_space_id`/`aget_by_department_id`/`adelete_by_space_id`(Task 1)、`DepartmentDao.aget_by_id`、`KnowledgeSpaceScopeDao.aget_space_ids_by_level('team')`（models/knowledge_space_scope.py:131）、`KnowledgeDao.async_get_spaces_by_ids`
- Produces（service classmethods，均 `login_user` 首参并 `_ensure_super_admin`）:
  - `bind_space_to_department(login_user, *, space_id, department_id) -> dict`
  - `unbind_space(login_user, *, space_id) -> None`
  - `list_bindings(login_user) -> list[dict]`（每项：space_id, space_name, department_id, department_name, created_by, create_time）
  - `list_bindable_team_spaces(login_user, *, keyword=None) -> list[dict]`（level=team 且未绑定：space_id, name）
  - `list_departments(login_user) -> list[dict]`（id, name）
- Produces（endpoints，均 `Depends(UserPayload.get_login_user)`）:
  - `POST /knowledge/space/department-binding` body `{space_id, department_id}`
  - `DELETE /knowledge/space/department-binding/{space_id}`
  - `GET /knowledge/space/department-binding/bindings`
  - `GET /knowledge/space/department-binding/bindable-spaces?keyword=`
  - `GET /knowledge/space/department-binding/departments`

绑定校验：目标 space 必须 level=team；space 未被绑定（`aget_by_space_id` 为 None，否则 `DepartmentKnowledgeSpaceExistsError`）；department 未被绑定（`aget_by_department_id` 为 None）；department 存在且 active。

- [ ] **Step 1: 写失败测试（service 层）**

创建 `src/backend/test/test_department_binding_admin.py`：

```python
import pytest
from types import SimpleNamespace
from unittest.mock import AsyncMock, patch

from bisheng.knowledge.domain.services.department_knowledge_space_service import (
    DepartmentKnowledgeSpaceService as Svc,
)

MOD = "bisheng.knowledge.domain.services.department_knowledge_space_service"


def _admin():
    return SimpleNamespace(user_id=1, tenant_id=1, is_admin=lambda: True)


def _non_admin():
    return SimpleNamespace(user_id=2, tenant_id=1, is_admin=lambda: False)


@pytest.mark.asyncio
async def test_bind_requires_admin():
    with pytest.raises(Exception):
        await Svc.bind_space_to_department(_non_admin(), space_id=10, department_id=3)


@pytest.mark.asyncio
async def test_bind_rejects_already_bound_space():
    with patch(f"{MOD}.KnowledgeSpaceScopeDao.aget_space_ids_by_level", new=AsyncMock(return_value=[10])), \
         patch(f"{MOD}.DepartmentKnowledgeSpaceDao.aget_by_space_id", new=AsyncMock(return_value=object())), \
         patch(f"{MOD}.DepartmentKnowledgeSpaceDao.aget_by_department_id", new=AsyncMock(return_value=None)), \
         patch(f"{MOD}.DepartmentDao.aget_by_id", new=AsyncMock(return_value=SimpleNamespace(id=3, name="d", status="active"))):
        with pytest.raises(Exception):
            await Svc.bind_space_to_department(_admin(), space_id=10, department_id=3)


@pytest.mark.asyncio
async def test_bind_success_creates_row():
    with patch(f"{MOD}.KnowledgeSpaceScopeDao.aget_space_ids_by_level", new=AsyncMock(return_value=[10])), \
         patch(f"{MOD}.DepartmentKnowledgeSpaceDao.aget_by_space_id", new=AsyncMock(return_value=None)), \
         patch(f"{MOD}.DepartmentKnowledgeSpaceDao.aget_by_department_id", new=AsyncMock(return_value=None)), \
         patch(f"{MOD}.DepartmentDao.aget_by_id", new=AsyncMock(return_value=SimpleNamespace(id=3, name="d", status="active"))), \
         patch(f"{MOD}.DepartmentKnowledgeSpaceDao.acreate", new=AsyncMock(return_value=SimpleNamespace(space_id=10, department_id=3))) as create:
        await Svc.bind_space_to_department(_admin(), space_id=10, department_id=3)
        create.assert_awaited_once()


@pytest.mark.asyncio
async def test_bind_rejects_non_team_space():
    with patch(f"{MOD}.KnowledgeSpaceScopeDao.aget_space_ids_by_level", new=AsyncMock(return_value=[])):  # 10 不在 team 集合
        with pytest.raises(Exception):
            await Svc.bind_space_to_department(_admin(), space_id=10, department_id=3)


@pytest.mark.asyncio
async def test_unbind_calls_dao():
    with patch(f"{MOD}.DepartmentKnowledgeSpaceDao.adelete_by_space_id", new=AsyncMock(return_value=True)) as d:
        await Svc.unbind_space(_admin(), space_id=10)
        d.assert_awaited_once_with(10)
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd /Users/zhangguoqing/works/bisheng_2-dept-knowledge-binding/src/backend && uv run pytest test/test_department_binding_admin.py -v`
Expected: FAIL —— `AttributeError: ... 'bind_space_to_department'`

- [ ] **Step 3: 实现 service 方法**

在 `DepartmentKnowledgeSpaceService` 里加（import `KnowledgeSpaceScopeDao`、`KnowledgeDao`、`DepartmentDao` 已在文件或补上；`DepartmentKnowledgeSpaceExistsError`/`DepartmentNotFoundError` 已 import）：

```python
    @classmethod
    async def bind_space_to_department(cls, login_user, *, space_id: int, department_id: int) -> dict:
        cls._ensure_super_admin(login_user)
        team_ids = set(await KnowledgeSpaceScopeDao.aget_space_ids_by_level(
            KnowledgeSpaceLevelEnum.TEAM))
        if int(space_id) not in team_ids:
            raise SpaceNotFoundError(msg="只能绑定团队知识库")
        if await DepartmentKnowledgeSpaceDao.aget_by_space_id(space_id) is not None:
            raise DepartmentKnowledgeSpaceExistsError(msg="该知识库已绑定部门")
        if await DepartmentKnowledgeSpaceDao.aget_by_department_id(department_id) is not None:
            raise DepartmentKnowledgeSpaceExistsError(msg="该部门已绑定科室知识库")
        dept = await DepartmentDao.aget_by_id(department_id)
        if dept is None or getattr(dept, "status", "active") != "active":
            raise DepartmentNotFoundError()
        row = await DepartmentKnowledgeSpaceDao.acreate(
            tenant_id=login_user.tenant_id, department_id=department_id,
            space_id=space_id, created_by=login_user.user_id,
        )
        return {"space_id": row.space_id, "department_id": row.department_id}

    @classmethod
    async def unbind_space(cls, login_user, *, space_id: int) -> None:
        cls._ensure_super_admin(login_user)
        await DepartmentKnowledgeSpaceDao.adelete_by_space_id(space_id)

    @classmethod
    async def list_bindings(cls, login_user) -> list:
        cls._ensure_super_admin(login_user)
        rows = await DepartmentKnowledgeSpaceDao.aget_all()
        if not rows:
            return []
        spaces = await KnowledgeDao.async_get_spaces_by_ids([r.space_id for r in rows])
        name_map = {s.id: s.name for s in spaces}
        dept_map = {d.id: d.name for d in await DepartmentDao.aget_by_ids([r.department_id for r in rows])}
        return [{
            "space_id": r.space_id, "space_name": name_map.get(r.space_id, ""),
            "department_id": r.department_id, "department_name": dept_map.get(r.department_id, ""),
            "created_by": r.created_by, "create_time": str(getattr(r, "create_time", "")),
        } for r in rows]

    @classmethod
    async def list_bindable_team_spaces(cls, login_user, *, keyword: str = None) -> list:
        cls._ensure_super_admin(login_user)
        team_ids = await KnowledgeSpaceScopeDao.aget_space_ids_by_level(KnowledgeSpaceLevelEnum.TEAM)
        if not team_ids:
            return []
        bound = {r.space_id for r in await DepartmentKnowledgeSpaceDao.aget_by_space_ids(team_ids)}
        free_ids = [i for i in team_ids if i not in bound]
        spaces = await KnowledgeDao.async_get_spaces_by_ids(free_ids) if free_ids else []
        result = [{"space_id": s.id, "name": s.name} for s in spaces]
        if keyword:
            kw = keyword.strip().lower()
            result = [x for x in result if kw in (x["name"] or "").lower()]
        return result

    @classmethod
    async def list_departments(cls, login_user) -> list:
        cls._ensure_super_admin(login_user)
        rows = await DepartmentDao.aget_all_active()  # 若无此方法，见 Step 3a
        return [{"id": d.id, "name": d.name} for d in rows]
```

- [ ] **Step 3a: 确认部门列表 DAO**

Run: `cd /Users/zhangguoqing/works/bisheng_2-dept-knowledge-binding/src/backend && grep -n "async def aget_all\|def aget_all_active\|async def alist" bisheng/database/models/department.py`
用实际存在的「取全部/active 部门」方法名替换 `aget_all_active`（若只有分页方法，取第一页足够大的 page_size，或加一个 `aget_all_active` 薄封装并补一条 DAO 测试）。

- [ ] **Step 4: 加 endpoints**

在 `knowledge_space.py`（department endpoint 群，行 300 之后）照 `batch_create_department_spaces`（300-314）的 `try/except BaseErrorCode` 风格加：

```python
@router.post("/department-binding")
async def bind_department_space(
    body: DepartmentBindingReq,
    login_user: UserPayload = Depends(UserPayload.get_login_user),
):
    try:
        data = await DepartmentKnowledgeSpaceService.bind_space_to_department(
            login_user, space_id=body.space_id, department_id=body.department_id)
        return resp_200(data)
    except BaseErrorCode as e:
        return e.return_resp_instance()


@router.delete("/department-binding/{space_id}")
async def unbind_department_space(
    space_id: int,
    login_user: UserPayload = Depends(UserPayload.get_login_user),
):
    try:
        await DepartmentKnowledgeSpaceService.unbind_space(login_user, space_id=space_id)
        return resp_200()
    except BaseErrorCode as e:
        return e.return_resp_instance()


@router.get("/department-binding/bindings")
async def list_department_bindings(
    login_user: UserPayload = Depends(UserPayload.get_login_user),
):
    try:
        return resp_200(await DepartmentKnowledgeSpaceService.list_bindings(login_user))
    except BaseErrorCode as e:
        return e.return_resp_instance()


@router.get("/department-binding/bindable-spaces")
async def list_bindable_spaces(
    keyword: str = None,
    login_user: UserPayload = Depends(UserPayload.get_login_user),
):
    try:
        return resp_200(await DepartmentKnowledgeSpaceService.list_bindable_team_spaces(
            login_user, keyword=keyword))
    except BaseErrorCode as e:
        return e.return_resp_instance()


@router.get("/department-binding/departments")
async def list_binding_departments(
    login_user: UserPayload = Depends(UserPayload.get_login_user),
):
    try:
        return resp_200(await DepartmentKnowledgeSpaceService.list_departments(login_user))
    except BaseErrorCode as e:
        return e.return_resp_instance()
```

`DepartmentBindingReq` 加到该文件的 schema 区（或 `knowledge/domain/schemas/knowledge_space_schema.py`）：

```python
class DepartmentBindingReq(BaseModel):
    space_id: int
    department_id: int
```

> 路由顺序注意：`/department-binding/...` 具体路径要放在 `/{space_id}` 之类通配路由**之前**注册，避免被吞。检查 `knowledge_space.py` 现有 `DELETE /{space_id}`（行 211）与新 `DELETE /department-binding/{space_id}` 的匹配优先级——FastAPI 按声明顺序匹配，具体路径需在前。

- [ ] **Step 5: 跑测试确认通过 + 冒烟导入**

Run: `cd /Users/zhangguoqing/works/bisheng_2-dept-knowledge-binding/src/backend && uv run pytest test/test_department_binding_admin.py -v`
Expected: PASS（5 passed）
Run: `cd /Users/zhangguoqing/works/bisheng_2-dept-knowledge-binding/src/backend && uv run python -c "import bisheng.knowledge.api.endpoints.knowledge_space"`
Expected: 无 import 错误

- [ ] **Step 6: 提交**

```bash
cd /Users/zhangguoqing/works/bisheng_2-dept-knowledge-binding
git add -A
git commit -m "feat(knowledge): 科室库绑定/解绑/列表/可绑空间/部门列表 admin API"
```

---

## Phase 2 — portal（BFF + 前端）

> 全部在 worktree `/Users/zhangguoqing/works/shougang-dept-knowledge-binding` 内。依赖 Phase 1 的 bisheng 接口契约（路径 `/api/v1/knowledge/space/department-binding/*`；注意 bisheng 对外网关前缀通常是 `/api/v1` + router 前缀 `/knowledge/space`，实际下游路径以 Phase 1 endpoint 为准，BFF 里按现有 `admin_config.py` 调用 bisheng 的 path 前缀习惯拼接）。

### Task 7: BFF 代理端点

**Files:**
- Modify: `backend/app/api/routes/admin_config.py`（加 5 个端点，挂在已受 `require_admin_session` 保护的 router 下）
- Test: `backend/tests/test_admin_config_api.py`（加用例 + 扩展 `FakeBishengClient`）

**Interfaces:**
- Consumes: `bisheng_client.get_json/post_json/delete_json`（`clients/bisheng.py:146/159/185`）、`response_ok`/`response_error`（`admin_config.py:20`）、`normalize_user_facing_message`（`services/error_messages.py:65`）、`get_bisheng_client`（`admin_config.py:11`）
- Produces（前缀 `/api/v1/admin/config`）:
  - `GET  /dept-knowledge-binding/bindings`
  - `GET  /dept-knowledge-binding/bindable-spaces?keyword=`
  - `GET  /dept-knowledge-binding/departments`
  - `POST /dept-knowledge-binding` body `{space_id, department_id}`
  - `DELETE /dept-knowledge-binding/{space_id}`
  - 下游 bisheng 路径常量（按 Phase 1 endpoint）：`/api/v1/knowledge/space/department-binding[...]`

- [ ] **Step 1: 扩展 FakeBishengClient + 写失败测试**

在 `backend/tests/test_admin_config_api.py` 的 `FakeBishengClient`（行 20 附近）里，给 `get_json`/`post_json` 补 `department-binding` 分支，并加 `delete_json`（记录 `delete_calls`）。然后加用例：

```python
def test_get_dept_bindings_proxies_bisheng(client):
    fake = FakeBishengClient()
    fake.bindings = [{"space_id": 10, "space_name": "团队库A",
                      "department_id": 3, "department_name": "研发部",
                      "created_by": 1, "create_time": "2026-07-06"}]
    client.app.state.bisheng_client = fake
    resp = client.get("/api/v1/admin/config/dept-knowledge-binding/bindings")
    assert resp.status_code == 200
    assert resp.json()["data"][0]["space_id"] == 10


def test_post_dept_binding_forwards_body(client):
    fake = FakeBishengClient()
    client.app.state.bisheng_client = fake
    resp = client.post("/api/v1/admin/config/dept-knowledge-binding",
                       json={"space_id": 10, "department_id": 3})
    assert resp.status_code == 200
    assert fake.post_calls[-1][1] == {"space_id": 10, "department_id": 3}


def test_delete_dept_binding_proxies(client):
    fake = FakeBishengClient()
    client.app.state.bisheng_client = fake
    resp = client.delete("/api/v1/admin/config/dept-knowledge-binding/10")
    assert resp.status_code == 200
    assert 10 in fake.delete_calls


def test_post_dept_binding_maps_bisheng_error(client):
    fake = FakeBishengClient()
    fake.bind_status = {"status_code": 500, "status_message": "该部门已绑定科室知识库"}
    client.app.state.bisheng_client = fake
    resp = client.post("/api/v1/admin/config/dept-knowledge-binding",
                       json={"space_id": 10, "department_id": 3})
    assert resp.status_code == 502
    assert "已绑定" in resp.json()["status_message"]
```

（在 `FakeBishengClient` 里：`get_json` 命中 `bindings`→`{"status_code":200,"data":self.bindings}`、`bindable-spaces`/`departments` 返回样例；`post_json` 命中 `department-binding`→记录 `post_calls` 并返回 `self.bind_status`（默认 `{"status_code":200,"data":{}}`）；`delete_json` 命中→记录 `delete_calls`。）

- [ ] **Step 2: 跑测试确认失败**

Run: `cd /Users/zhangguoqing/works/shougang-dept-knowledge-binding/backend && pytest tests/test_admin_config_api.py -k dept_binding -v`
Expected: FAIL —— 404（端点不存在）

- [ ] **Step 3: 实现 BFF 端点**

在 `admin_config.py` 末尾加（照 `get_space_files` 242 + `update_domains_config` 302 的错误处理范式）：

```python
_DEPT_BINDING_BASE = "/api/v1/knowledge/space/department-binding"


@router.get("/dept-knowledge-binding/bindings")
async def get_dept_bindings(bisheng_client: BishengClient = Depends(get_bisheng_client)):
    resp = await bisheng_client.get_json(f"{_DEPT_BINDING_BASE}/bindings")
    if resp.get("status_code") not in (None, 200):
        return response_error(normalize_user_facing_message(
            resp.get("status_message"), fallback="获取科室库绑定失败", status_code=502), status_code=502)
    return response_ok(resp.get("data") or [])


@router.get("/dept-knowledge-binding/bindable-spaces")
async def get_dept_bindable_spaces(keyword: str = None,
                                   bisheng_client: BishengClient = Depends(get_bisheng_client)):
    resp = await bisheng_client.get_json(f"{_DEPT_BINDING_BASE}/bindable-spaces",
                                         params={"keyword": keyword} if keyword else None)
    if resp.get("status_code") not in (None, 200):
        return response_error("获取可绑定知识库失败", status_code=502)
    return response_ok(resp.get("data") or [])


@router.get("/dept-knowledge-binding/departments")
async def get_dept_departments(bisheng_client: BishengClient = Depends(get_bisheng_client)):
    resp = await bisheng_client.get_json(f"{_DEPT_BINDING_BASE}/departments")
    if resp.get("status_code") not in (None, 200):
        return response_error("获取部门列表失败", status_code=502)
    return response_ok(resp.get("data") or [])


@router.post("/dept-knowledge-binding")
async def bind_dept_space(body: dict,
                          bisheng_client: BishengClient = Depends(get_bisheng_client)):
    resp = await bisheng_client.post_json(_DEPT_BINDING_BASE, json={
        "space_id": body.get("space_id"), "department_id": body.get("department_id")})
    if resp.get("status_code") not in (None, 200):
        return response_error(normalize_user_facing_message(
            resp.get("status_message"), fallback="绑定失败", status_code=502), status_code=502)
    return response_ok(resp.get("data") or {})


@router.delete("/dept-knowledge-binding/{space_id}")
async def unbind_dept_space(space_id: int,
                            bisheng_client: BishengClient = Depends(get_bisheng_client)):
    resp = await bisheng_client.delete_json(f"{_DEPT_BINDING_BASE}/{space_id}")
    if resp.get("status_code") not in (None, 200):
        return response_error(normalize_user_facing_message(
            resp.get("status_message"), fallback="解绑失败", status_code=502), status_code=502)
    return response_ok(resp.get("data") or {})
```

> 确认 `normalize_user_facing_message` 的调用签名（`error_messages.py:65`）——若首参要求 `Exception` 而非 str，则传 `RuntimeError(resp.get("status_message") or "...")`。

- [ ] **Step 4: 跑测试确认通过**

Run: `cd /Users/zhangguoqing/works/shougang-dept-knowledge-binding/backend && pytest tests/test_admin_config_api.py -k dept_binding -v`
Expected: PASS（4 passed）

- [ ] **Step 5: 提交**

```bash
cd /Users/zhangguoqing/works/shougang-dept-knowledge-binding
git add backend/app/api/routes/admin_config.py backend/tests/test_admin_config_api.py
git commit -m "feat(bff): 科室库绑定 admin 代理端点(bindings/bindable/departments/bind/unbind)"
```

---

### Task 8: 前端 API 封装 + 类型

**Files:**
- Modify: `frontend/src/api/adminConfig.ts`（顶部类型区 3-281；API 封装区）

**Interfaces:**
- Consumes: `request<T>(path, init)`（`adminConfig.ts:283-319`）
- Produces:
  - 类型 `DeptBinding`、`BindableSpace`、`DepartmentOption`
  - `fetchDeptBindings()`, `fetchBindableSpaces(keyword?)`, `fetchBindingDepartments()`, `bindDeptSpace(spaceId, departmentId)`, `unbindDeptSpace(spaceId)`

- [ ] **Step 1: 加类型（顶部类型区）**

```typescript
export interface DeptBinding {
  space_id: number;
  space_name: string;
  department_id: number;
  department_name: string;
  created_by?: number;
  create_time?: string;
}
export interface BindableSpace { space_id: number; name: string; }
export interface DepartmentOption { id: number; name: string; }
```

- [ ] **Step 2: 加 API 封装（照 `fetchSpaceOptions`/`updateDomainsConfig` 375-392）**

```typescript
export function fetchDeptBindings() {
  return request<DeptBinding[]>('/api/v1/admin/config/dept-knowledge-binding/bindings');
}
export function fetchBindableSpaces(keyword?: string) {
  const q = keyword ? `?keyword=${encodeURIComponent(keyword)}` : '';
  return request<BindableSpace[]>(`/api/v1/admin/config/dept-knowledge-binding/bindable-spaces${q}`);
}
export function fetchBindingDepartments() {
  return request<DepartmentOption[]>('/api/v1/admin/config/dept-knowledge-binding/departments');
}
export function bindDeptSpace(spaceId: number, departmentId: number) {
  return request<Record<string, unknown>>('/api/v1/admin/config/dept-knowledge-binding', {
    method: 'POST', body: JSON.stringify({ space_id: spaceId, department_id: departmentId }),
  });
}
export function unbindDeptSpace(spaceId: number) {
  return request<Record<string, unknown>>(`/api/v1/admin/config/dept-knowledge-binding/${spaceId}`, {
    method: 'DELETE',
  });
}
```

- [ ] **Step 3: 类型检查**

Run: `cd /Users/zhangguoqing/works/shougang-dept-knowledge-binding/frontend && npx tsc --noEmit`
Expected: 无新增类型错误

- [ ] **Step 4: 提交**

```bash
cd /Users/zhangguoqing/works/shougang-dept-knowledge-binding
git add frontend/src/api/adminConfig.ts
git commit -m "feat(web): 科室库绑定 admin API 封装 + 类型"
```

---

### Task 9: 前端绑定校验纯函数 + 单测

**Files:**
- Create: `frontend/src/utils/deptKnowledgeBinding.ts`
- Test: `frontend/tests/deptKnowledgeBinding.test.ts`

**Interfaces:**
- Produces:
  - `interface BindingDraft { spaceId: number | null; departmentId: number | null; }`
  - `createBindingDraft(): BindingDraft`
  - `validateBindingDraft(draft): string | null`（返回错误信息或 null）
  - `groupBindingsByDepartment(bindings: DeptBinding[]): DeptBinding[]`（按 department_name 排序，便于列表展示）

- [ ] **Step 1: 写失败测试**

创建 `frontend/tests/deptKnowledgeBinding.test.ts`：

```typescript
import { test } from 'node:test';
import assert from 'node:assert/strict';
import {
  createBindingDraft, validateBindingDraft, groupBindingsByDepartment,
} from '../src/utils/deptKnowledgeBinding';

test('createBindingDraft is empty', () => {
  assert.deepEqual(createBindingDraft(), { spaceId: null, departmentId: null });
});

test('validate requires both', () => {
  assert.ok(validateBindingDraft({ spaceId: null, departmentId: 3 }));
  assert.ok(validateBindingDraft({ spaceId: 10, departmentId: null }));
  assert.equal(validateBindingDraft({ spaceId: 10, departmentId: 3 }), null);
});

test('groupBindingsByDepartment sorts by department name', () => {
  const rows = [
    { space_id: 2, space_name: 'B', department_id: 2, department_name: '乙部' },
    { space_id: 1, space_name: 'A', department_id: 1, department_name: '甲部' },
  ];
  const sorted = groupBindingsByDepartment(rows as any);
  assert.equal(sorted[0].department_name, '甲部');
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd /Users/zhangguoqing/works/shougang-dept-knowledge-binding/frontend && npm test`
Expected: FAIL —— 找不到模块 `deptKnowledgeBinding`

- [ ] **Step 3: 实现**

创建 `frontend/src/utils/deptKnowledgeBinding.ts`：

```typescript
import type { DeptBinding } from '../api/adminConfig';

export interface BindingDraft {
  spaceId: number | null;
  departmentId: number | null;
}

export function createBindingDraft(): BindingDraft {
  return { spaceId: null, departmentId: null };
}

export function validateBindingDraft(draft: BindingDraft): string | null {
  if (draft.spaceId == null) return '请选择要绑定的团队知识库';
  if (draft.departmentId == null) return '请选择部门';
  return null;
}

export function groupBindingsByDepartment(bindings: DeptBinding[]): DeptBinding[] {
  return [...bindings].sort((a, b) =>
    (a.department_name || '').localeCompare(b.department_name || '', 'zh-Hans-CN'));
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd /Users/zhangguoqing/works/shougang-dept-knowledge-binding/frontend && npm test`
Expected: PASS（含新增 3 个用例）

- [ ] **Step 5: 提交**

```bash
cd /Users/zhangguoqing/works/shougang-dept-knowledge-binding
git add frontend/src/utils/deptKnowledgeBinding.ts frontend/tests/deptKnowledgeBinding.test.ts
git commit -m "feat(web): 科室库绑定 draft/校验/分组纯函数 + 单测"
```

---

### Task 10: AdminPage「科室知识库绑定」面板

**Files:**
- Modify: `frontend/src/pages/AdminPage.tsx`（照「业务域 DomainConfig」面板改造）

**Interfaces:**
- Consumes: Task 8 的 API、Task 9 的 draft/校验、`runSave`（`AdminPage.tsx:396`）
- 参照范本行号（业务域）：`NAV_ITEMS` 122-138；状态 262-270；`loadSpaceOptions`/`openXxxDialog` 408-436；面板渲染 805-816；弹窗挂载 1052-1100；`DomainsTable` 1804-1888；`DomainEditorDialog` 1907+；`persistDomains` 4810；`handleAdd/Edit/DeleteDomain` 4868-4924。

**与业务域面板的关键差异**：绑定是「单条 bind/unbind 后 refetch 列表」，**不是整表 POST 覆盖**。所以：
- 新增：调 `bindDeptSpace` 成功 → `await refetchBindings()`
- 删除（解绑）：调 `unbindDeptSpace` 成功 → `await refetchBindings()`
- 进入面板时并行拉 `fetchDeptBindings` / `fetchBindableSpaces` / `fetchBindingDepartments`

- [ ] **Step 1: 加导航项**

在 `NAV_ITEMS`（122-138）加一项 `{ key: 'deptBinding', label: '科室知识库绑定' }`（label/icon 照相邻项风格）。`NavKey` 会自动含新 key。

- [ ] **Step 2: 加状态 + 加载函数**

在状态区（262-270 附近）加：

```typescript
const [deptBindings, setDeptBindings] = useState<DeptBinding[]>([]);
const [bindableSpaces, setBindableSpaces] = useState<BindableSpace[]>([]);
const [bindingDepartments, setBindingDepartments] = useState<DepartmentOption[]>([]);
const [bindingDraft, setBindingDraft] = useState<BindingDraft>(createBindingDraft());
const [bindingDialogOpen, setBindingDialogOpen] = useState(false);
```

加加载 + 刷新（放在其它 `loadXxx` 附近，408-436）：

```typescript
const refetchBindings = useCallback(async () => {
  const [b, s, d] = await Promise.all([
    fetchDeptBindings(), fetchBindableSpaces(), fetchBindingDepartments(),
  ]);
  setDeptBindings(b); setBindableSpaces(s); setBindingDepartments(d);
}, []);
```

在切到该面板的 effect 里调用 `refetchBindings()`（照现有 `active === 'domains'` 时 `loadSpaceOptions` 的触发方式）。

- [ ] **Step 3: 加提交/解绑 handler**

照 `handleAddDomain`（4868+）用 `runSave` 包装：

```typescript
const handleBindSpace = () => {
  const err = validateBindingDraft(bindingDraft);
  if (err) { setError(err); return; }
  runSave(async () => {
    await bindDeptSpace(bindingDraft.spaceId!, bindingDraft.departmentId!);
    await refetchBindings();
    setBindingDialogOpen(false);
    setBindingDraft(createBindingDraft());
  });
};

const handleUnbindSpace = (spaceId: number) => {
  runSave(async () => {
    await unbindDeptSpace(spaceId);
    await refetchBindings();
  });
};
```

- [ ] **Step 4: 加面板渲染 + 弹窗**

在面板渲染区（805-816 的 `active === 'domains'` 之后）加 `active === 'deptBinding'` 分支，渲染一个 `DeptBindingTable`（列：知识库名 / 部门 / 创建时间 / 解绑按钮，数据 `groupBindingsByDepartment(deptBindings)`）+「新增绑定」按钮（打开 `bindingDialogOpen`）。照 `DomainsTable`（1804-1888）结构写一个 `DeptBindingTable` 组件；弹窗照 `DomainEditorDialog` 写一个 `DeptBindingDialog`（两个下拉：可绑定空间 `bindableSpaces`、部门 `bindingDepartments`；提交调 `handleBindSpace`）。解绑走确认弹窗（照 `DomainDeleteDialog` 1086-1100）。

顶部补 import：

```typescript
import {
  fetchDeptBindings, fetchBindableSpaces, fetchBindingDepartments,
  bindDeptSpace, unbindDeptSpace,
  type DeptBinding, type BindableSpace, type DepartmentOption,
} from '../api/adminConfig';
import {
  createBindingDraft, validateBindingDraft, groupBindingsByDepartment,
  type BindingDraft,
} from '../utils/deptKnowledgeBinding';
```

- [ ] **Step 5: 类型检查 + 构建冒烟**

Run: `cd /Users/zhangguoqing/works/shougang-dept-knowledge-binding/frontend && npx tsc --noEmit && npm run build`
Expected: 类型通过、构建成功

- [ ] **Step 6: 提交**

```bash
cd /Users/zhangguoqing/works/shougang-dept-knowledge-binding
git add frontend/src/pages/AdminPage.tsx
git commit -m "feat(web): 后台新增科室知识库绑定面板"
```

---

## 端到端手工验证（两仓库都起服务后）

1. bisheng 后台把 A 团队库绑定给「研发部」→ portal 后台绑定列表出现该项。
2. 建自由 team 库 B（创建者主部门=研发部），上传若干文件。
3. 在 bisheng 删除 B → 返回「迁移中」，稍后 B 消失、文件出现在 A（含向量可检索），md5 重复文件不重复。
4. 阻断验证：删除已绑定的 A → 报「科室库禁止删除请先解绑」；创建者无主部门的自由库删除 → 报「无法迁移」；解绑后再删 A → 正常删除。

---

## Self-Review 结果

**Spec 覆盖检查**（对照 spec §4 决策表 D1–D10）：
- D1 给现有 team 库加绑定行 → Task 6 `bind_space_to_department`（`acreate` 传现有 space_id）✅
- D2 portal 后台入口 → Task 7–10 ✅
- D3 自动挂钩 delete_space → Task 5 ✅
- D4 无法迁移阻断 → Task 3 guard `block` + Task 5 抛 errcode ✅
- D5 科室库禁止删除 → Task 3 `binding 非 None → block` ✅
- D6 沿 path 向上找 → Task 2 `_department_id_chain` ✅
- D7 异步迁移+成功删源库+失败回滚 → Task 4 ✅
- D8 embedding 不一致阻断 → Task 3 `target.model != space.model` ✅
- D9 不改权限 → 计划全程未触碰权限授予 ✅
- D10 level 保持 team → 绑定只写 `department_knowledge_space`，未改 `knowledge_space_scope` ✅
- spec §8.3 迁移锁 → 采用方案(b) `state=COPYING`，Task 3 检测 + Task 5 置位 + Task 4 回滚 ✅（无 alembic，已在 Global Constraints 声明）

**占位扫描**：无 TBD/TODO；Step 3a / errcode 号段 / normalize 签名 等标注为「运行时确认」的点都给了确认命令与回退方案，非占位。

**类型一致性**：`MigrationDecision.action/target_space_id/reason` 在 Task 3 定义、Task 5 消费一致；`space_migrate_celery({source_id,target_id,op_user_id})` 投递(Task 5)与消费(Task 4)键名一致；前端 `DeptBinding/BindableSpace/DepartmentOption` 在 Task 8 定义、Task 9/10 消费一致；`bind/unbind` 端点路径 portal(Task 7) 与 bisheng(Task 6) 对应。

**已知需实现时确认的下游细节**（非阻塞，已在对应 Step 标注命令）：errcode 基类/文件路径(3-Step1)、部门列表 DAO 名(6-Step3a)、`normalize_user_facing_message` 入参类型(7-Step3)、`knowledge_space_service.py` 内 mock 符号名(5-Step1)、FastAPI 路由匹配顺序(6-Step4)。
